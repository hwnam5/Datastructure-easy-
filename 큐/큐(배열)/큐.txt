큐
- 큐(Queue)는 뒤쪽으로 들어가서 앞쪽으로 나오는 자료구조(data structure)이다.
push : 큐에 데이터를 넣는다
pop : 큐에서 데이터를 빼낸다.

큐의 구현
배열을 이용한 방법, 연결 리스트를 이용한 구현 방법

큐(연결리스트)
삽입과정
1) 삽입할 노드 마련
2) 원래 rear노드의 next가 삽입할 노드를 가리키게 한다
3) 삽입한 노드가 rear이 되게 한다

추출과정
1) front를 삭제할 노드의 next로 옮긴다
2) 삭제할 노드 메모리 해제